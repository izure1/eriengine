import Phaser from 'phaser'

{{ DATA_LISTS }}


/** 오디오의 고유값입니다. 다른 오디오와 중복되어선 안됩니다. 잘 모르겠다면 건드리지 마십시오. */
export const key: string        = __filename

/** 오디오에 사용할 오디오에셋입니다. */
export const asset: string      = Assets[]