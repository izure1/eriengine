import Assets from '@/ASSET_LIST'


/** 이미지의 고유값입니다. 다른 이미지와 중복되어선 안됩니다. 잘 모르겠다면 건드리지 마십시오. */
export const key: string        = __filename

/** 이미지에 사용할 이미지에셋입니다. */
export const asset: string      = Assets[]